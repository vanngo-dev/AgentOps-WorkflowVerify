export default function WorkflowRunsPage() {
  return (
    <section className="page-grid" aria-labelledby="workflow-runs-title">
      <div className="page-heading">
        <p className="eyebrow">Workflows</p>
        <h2 id="workflow-runs-title">Workflow Runs</h2>
        <p>Dashboard data will be connected in the next phase.</p>
      </div>
      <div className="placeholder-panel">
        <span>Placeholder</span>
        <strong>No workflow runs loaded yet.</strong>
      </div>
    </section>
  );
}
